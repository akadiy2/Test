import java.util.*;
public class Practice {
	
	public static void main (String [] args) {
		Scanner sc = new Scanner (System.in);
		System.out.print("Enter two numbers: ");
		int x = sc.nextInt();
		int y = sc.nextInt();
		int f = gcd(x,y);
		System.out.println("F(nbr) = "+f);
	}
	
	public static int gcd(int x, int y) {
		if (y == 0) return x;
		return gcd(y,x%y);
	}
	
	

}
